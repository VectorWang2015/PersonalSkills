---
name: {{skill-slug}}
description: >-
  {{用一至三句话写清可识别的用户情境、该 skill 产生的结果，以及一个关键反场景。不要只写主题名，也不要罗列内部方法论。}}
metadata:
  book2skill_capability_id: "{{capability-id}}"
  book2skill_revision: "{{revision}}"
  book2skill_source_manifest: "{{source-manifest-relative-path}}"
# 仅在确有需要时添加以下受支持的顶层字段；否则删除注释，不要添加自定义顶层字段：
# license: {{SPDX 标识或许可证文件引用}}
# allowed-tools: {{以空格分隔的工具名}}
---

<!--
生成最终 SKILL.md 时删除本注释块。

渐进披露原则：
1. frontmatter 只负责发现；description 包含“何时用 + 产生什么结果 + 何时不用”。
2. 正文先给最短可执行路径，再给判断规则和停止条件；步骤数量由真实依赖决定，不固定。
3. 原文、书中案例、完整批判和验证记录放入 references/capabilities/{{capability-slug}}.json；
   仅在需要解释依据、处理边界案例或审计时读取。
4. 删除不改变执行决策的背景、修辞和重复说明。
-->

# {{Skill Title}}

{{一句话说明此 skill 帮用户完成什么决策或任务。}}

## Use this skill

Use when:

- {{可观察的场景或用户意图 1}}
- {{可观察的场景或用户意图 2}}

Do not use when:

- {{关键反场景；指出应直接回答、询问澄清或改用哪个相邻 skill}}

Expected result: {{具体交付物或决策结果，不写“帮助用户理解”之类不可检验表述。}}

## Workflow

1. **{{首个必要动作}}** — {{所需输入与可判断的完成标准。}}
2. **{{下一必要动作；若一步即可闭环则删除本项，按实际依赖增删后续步骤}}** — {{处理或输出规则与可判断的完成标准。}}

## Decision rules

- {{决定如何应用方法论的核心规则 1}}
- {{核心规则 2，包括重要假设或权衡}}
- {{遇到信息不足时应如何处理}}

## Stop or escalate

- Stop when {{已达到充分答案或继续分析收益很低的条件。}}
- Ask the user when {{缺失信息会实质改变结论的条件。}}
- Do not continue when {{来源方法不适用、风险超出范围或需要专业判断的条件。}}

## Output contract

Return:

- {{必需输出 1}}
- {{必需输出 2}}
- {{不确定性、假设或反例的披露方式}}

## Optional depth

- For source excerpts, the author's examples, limitations, and verification evidence, read [the capability card](references/capabilities/{{capability-slug}}.json). Load it only when the task needs grounding or auditability.
- For routing behavior and near-neighbor negatives, use `test-prompts.json` during evaluation; do not load the test suite during ordinary execution.

## Related skills

- Depends on: {{无则写 None；有则写相对链接及依赖原因。}}
- Prefer instead: {{相邻 skill 的相对链接与区分条件；无则写 None。}}
- Compose with: {{可组合 skill 的相对链接与组合顺序；无则写 None。}}
