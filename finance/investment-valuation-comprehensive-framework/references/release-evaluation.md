# 发布评测记录

截至 2026-09-03，独立匿名聚合评测解盲结果为：

| 评测臂 | 分数 |
|---|---:|
| candidate | 99.5 |
| old skill | 99 |
| no skill | 97.5 |

该结果支持公开 standalone 入口及其公开 capability card 以 `active` / `supported` 状态发布，包级 `behavioral-evaluation` 记为 `passed`。

评测作用域是完整公开入口，不提供八张 `reference_only` 卡的逐卡归因；这些内部卡继续保持 `draft`，其 `incremental_utility` 继续保持 `hypothesis`。可见开发回归集不冒充本次匿名留出，未来 revision 仍需新的隔离评测。
